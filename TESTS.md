## Automated Tests

File: __tests__/audit.test.ts

1. calculates savings correctly
2. returns zero when already optimal
3. handles empty input
4. handles large spend
5. never returns negative savings

Run with:
npm test